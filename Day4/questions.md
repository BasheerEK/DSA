Great 👍
Here are **practice questions ONLY** based on the **string topics you shared**.
They are arranged from **easy → medium → hard**, perfect for a beginner doing DSA/strings practice.

---

# 🧵 Python String Practice Questions

## 🔹 Level 1: Basics & Indexing

1. Create a string `"Programming"` and:

   * Print the **first character**
   * Print the **last character**
   * Print the **middle character**

2. Given `text = "Python"`

   * Print characters at index `1`, `3`, and `-2`

3. Write a program to print **each character of a string** on a new line using a loop.

4. Find the **length of a string** without using `len()`.

5. Take a string input and print it in **reverse order** using slicing.

---

## 🔹 Level 2: Slicing Practice

6. Given `text = "Python Programming"`:

   * Extract `"Python"`
   * Extract `"Programming"`
   * Extract `"Pto rgamn"` using step slicing

7. Print the string except:

   * First character
   * Last character
   * First and last character

8. Print only **even index characters** of a string.

9. Print only **odd index characters** of a string.

10. Reverse a string **without using slicing**.

---

## 🔹 Level 3: String Concatenation & Formatting

11. Join two strings `"Hello"` and `"World"` using:

* `+` operator
* `f-string`
* `format()` method

12. Convert a list `['Python', 'is', 'fun']` into a single string `"Python is fun"`.

13. Take `name` and `age` as input and print:

```
My name is <name> and I am <age> years old
```

using **f-string**.

14. Print a floating number `3.14159` up to **2 decimal places** using string formatting.

---

## 🔹 Level 4: String Methods (Case & Search)

15. Convert a string to:

* Uppercase
* Lowercase
* Title case

16. Count how many times `'a'` appears in `"banana"`.

17. Find the index of `'Pro'` in `"Python Programming"`.

18. Check whether a string:

* Starts with `"Py"`
* Ends with `"ing"`

19. Check if `'Java'` exists in `"I love Python"`.

---

## 🔹 Level 5: Modification & Validation

20. Remove extra spaces from `"   Hello World   "`.

21. Replace `'Python'` with `'Java'` in `"Python Programming"`.

22. Split the string `"apple,banana,orange"` into a list.

23. Join the list `['2025', '06', '15']` using `'-'`.

24. Check whether the following are valid:

* `"12345"` → digits only?
* `"abcXYZ"` → alphabets only?
* `"abc123"` → alphanumeric?

---

## 🔹 Level 6: Logical String Problems

25. Check if a given string is a **palindrome**.

26. Count the **number of vowels** in a string.

27. Count the **number of words** in a sentence.

28. Find the **most frequent character** in a string.

29. Remove all **spaces** from a string.

30. Convert `"hello world"` → `"Hello World"` (title case manually).

---

## 🔹 Level 7: Advanced Practice

31. Reverse **each word** in `"Python is fun"` → `"nohtyP si nuf"`

32. Find all positions of character `'m'` in `"programming"`.

33. Check if two strings are **anagrams**
    (`"listen"` and `"silent"`).

34. Print characters that appear **only once** in a string.

35. Compress a string
    `"aaabbc"` → `"a3b2c1"`

---

## 🔹 Challenge Questions (🔥)

36. Remove duplicate characters from `"programming"`.

37. Find the **longest word** in a sentence.

38. Check if a string contains **only special characters**.

39. Swap the case of each character **without using `swapcase()`**.

40. Implement your own version of:

* `upper()`
* `lower()`
* `count()`

---

If you want next:

* ✅ **Answers**
* ✅ **Hints only**
* ✅ **Mini test (10 questions)**
* ✅ **Real interview-style string problems**

Just tell me 👍
